# next-tailwind-sqlite template

Opinionated production defaults: Next.js 15 (App Router, TS strict) +
Tailwind 4 + SQLite. Dark theme base. The builder workers extend this —
they never introduce a second styling system or framework.

Placeholders replaced at `salarymen init`:
- `{{PROJECT_NAME}}` → directory name
